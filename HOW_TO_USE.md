# استخدام G-MES

> ## ⚠️ تنبيه — `src/gmes` مُجمَّد الآن (قرار 2026-09-13)
>
> المحرك المعتمد للإنتاج أصبح المحرك القديم (الملفات المسطحة في جذر
> المشروع: `gmes_core.py`، `gmes_login.py`، `gmes_common.py`،
> `gmes_open_screen.py`، `cdp_common.py`). أما `src/gmes` فهو مُجمَّد
> بانتظار الإزالة على مراحل: **ممنوع إضافة مزايا له، وممنوع إصلاح أخطاء
> فيه، وممنوع تشغيله.**
>
> السبب في منع التشغيل: المحركان يستخدمان نفس منفذ CDP رقم 9444 ونفس نسخة
> بروفايل كروم `%LOCALAPPDATA%\Google\Chrome\CDP Profile`، فتشغيل الجديد
> قد يغيّر حالة المتصفح التي يعتمد عليها القديم لاحقًا.
>
> الأوامر المذكورة أدناه (`gmes.bat` و`GMES_Workflow.bat`) ما زالت تشغّل
> المحرك الجديد حتى تُنفَّذ الخطوة 3 من خطة الاستعادة في
> [CURRENT_STATE.md](CURRENT_STATE.md). المدخل الوحيد المُثبَت للمحرك القديم
> حاليًا هو `GMES_Workflow_LEGACY_TEST.bat`.
>
> المرجع الحاكم: القسم 0 في [CLAUDE.md](CLAUDE.md).

المسار المدعوم هو محرك واحد داخل `src/gmes`. شغّل `GMES_Workflow.bat` من
دون أوامر لأسئلة موجهة، أو شغّل `gmes.bat` للأوامر المباشرة. الاسم القديم
`run_gmes_workflow.py` يصل إلى نفس المحرك؛ لا توجد نسخة تنفيذ ثانية.

## قبل أول تشغيل

```powershell
python -m pip install -r requirements.txt
.\gmes.bat credentials set
.\gmes.bat doctor
```

الأمر `credentials set` يحفظ بيانات الدخول محليًا باستخدام Windows DPAPI.
الأمر `doctor` فحص قراءة فقط ولا يغيّر الملفات أو المتصفح.

## تشغيل تقرير

للواجهة السهلة اضغط مرتين على:

```powershell
.\GMES_Workflow.bat
```

بعد كتابة رقم الشاشة يفتحها البرنامج **قبل** أي سؤال، ثم يعرض كل ما فيها:

- **كل الفلاتر** الموجودة في اللوحة اليسرى، مرقّمة، مع الاسم الذي يفهمه
  المحرك والقيمة الموجودة فيها الآن.
- **كل الخيارات** الجانبية مع حالتها (`selected` / `not selected`)، لأن
  بعضها يغيّر الصفحة نفسها: Org/Prod/Fac/Proc، و Plan Date مقابل
  Create Date الذي يغيّر معنى التاريخ بالكامل.
- **الأقسام** التي يمكن اختيارها فعلًا، وشاشات Quick View مع تنبيه أنها
  شاشات أخرى لها كود مستقل وليست خيارًا هنا.

الاختيار بالرقم (`1` لفلتر، `o3` لخيار) أو بالاسم. يمكن ضبط أكثر من فلتر
واحدًا تلو الآخر، وترك القيمة فارغة يحذف الفلتر المحفوظ.

أو استخدم الأمر المباشر نفسه عند الحاجة:

```powershell
.\GMES_Workflow.bat run P1112UM00 --division VD --from 20260909 --to 20260909 --verify planYmd
```

- استخدم `--export xlsx` أو `csv` أو `both` أو `none`.
- استخدم `--output-dir "D:\Reports"` لاختيار مكان الحفظ.
- استخدم `--dry-run` لضبط الشاشة فقط من دون تنفيذ الاستعلام.
- عند تحديد تاريخ، `--verify` إلزامي حتى لا يُصدَّر تقرير قديم أو بتاريخ مختلف.
- إذا وُجد أكثر من جدول نتائج أو أكثر من شجرة للقسم، حدّد `--grid` أو `--tree` بدل التخمين.
- لو قال إن شكل الشاشة اتغيّر ورفض يشتغل بالمحفوظ: بُصّ على الشاشة الأول، وبعدين
  استخدم `--relearn` (أو جاوب `r` في الأسئلة الموجهة) علشان يسجّلها من جديد.
- لو شاشة فشلت 3 مرات ورا بعض، البرنامج بيتجاوزها تلقائيًا بدل ما يضيع وقت
  الليلة كلها عليها، وباقي الشاشات بتكمل عادي. استخدم `--force` (أو جاوب `f`
  في الأسئلة الموجهة) علشان تجربها تاني بعد ما تتأكد إنها اتصلحت.
- لو الجهاز اتقفل أو البرنامج اتقفل فجأة نص الشغل، تشغيله تاني بنفس الأمر
  بالظبط بيكمل من الشاشة اللي وقف عندها، مش من الأول - الشاشات اللي خلصت
  وصدّرت ملف فعلاً ما بتتكررش. استخدم `--fresh` لو عايز يبدأ من الأول قصدًا.
- للتشغيل الليلي بدون حد يراقب، استخدم `gmes supervise run ...` بدل
  `gmes run ...` مباشرة: لو البرنامج نفسه علّق تمامًا (مش فشل عادي، تعليق
  حقيقي) لمدة طويلة، البرنامج المراقب بيوقفه ويشغّله من جديد مرة واحدة
  تلقائيًا بدل ما يفضل واقف لحد الصبح.
- لو عايز تعرف بالفشل بالليل نفسه بدل ما تفتح اللوج الصبح، حدّد
  `GMES_ALERT_SMTP_HOST` و `GMES_ALERT_TO` (وممكن كمان `_PORT`/`_FROM`)
  كمتغيرات بيئة، وهيبعتلك إيميل بس لما تشغيلة تفشل - مفيش إيميل على
  النجاح. لو السيرفر محتاج تسجيل دخول، احفظه بـ
  `gmes credentials set-alert-smtp` (مش متغير بيئة - ده بيتشفر بنفس طريقة
  باسورد الـ G-MES، في ملف منفصل).

مثال لقراءة البيانات دون تصدير:

```powershell
.\gmes.bat data forms
.\gmes.bat data read P1112WM00 dsMasterProdPlan --limit 20
```

## ما الذي يحدث عند الخطأ

البرنامج يتوقف عند أي عدم يقين في الشاشة أو المرشحات أو التاريخ أو التصدير.
لا يشغّل التقرير التالي بعد فشل تقرير سابق.

نافذة الإشعارات (Notice) تُغلق تلقائيًا في ثلاث نقاط — عند فتح الشاشة، وقبل
Inquiry، وقبل زر الإكسل — وإذا لم تستجب لزر الإغلاق يُستخدم إغلاق Nexacro
نفسه. أي نافذة لا يستطيع البرنامج التعرّف عليها لا يلمسها ويذكرها في السجل
بدل الضغط عليها. تنزيل الإكسل يعيد المحاولة ثلاث مرات، وفي كل مرة يعيد
الشاشة إلى المقدمة ويزيح ما فوقها، لأن التشغيل الليلي يتم بلا أحد في المكتب.

تُحفظ السجلات ولقطات الفشل تحت:

```text
%LOCALAPPDATA%\GMES\
```

لا يغلق البرنامج متصفح Chrome الشخصي؛ يستعمل نسخة CDP منفصلة من ملف التعريف.
